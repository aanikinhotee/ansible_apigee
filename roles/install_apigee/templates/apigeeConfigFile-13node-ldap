# For OpenLDAP on IP4 and IP5
IP1=dev-project33 # zookeeper and cassandra
IP2=dev-project33 # zookeeper and cassandra
IP3=dev-project33 # zookeeper and cassandra
IP4=dev-project33 # ldap 
IP5=dev-project33 # ldap
IP6=dev-project33 # managemnnt1
IP7=dev-project33 # management2
IP8=dev-project33 # pg-master
IP9=dev-project33 # pg-standby
HOSTIP=$(hostname -i)
ENABLE_SYSTEM_CHECK=y
ADMIN_EMAIL=opdk@google.com
APIGEE_ADMINPW=ADMIN_PASSWORD1a
# First OpenLDAP Server on IP4
MSIP=$IP6
USE_LDAP_REMOTE_HOST=n
LDAP_TYPE=2
LDAP_SID=1
LDAP_PEER=$IP5
# Second OpenLDAP Server on IP5
# MSIP=$IP7
# USE_LDAP_REMOTE_HOST=n
# LDAP_TYPE=2
# LDAP_SID=2
# LDAP_PEER=$IP4
# Set same password for both OpenLDAPs.
APIGEE_LDAPPW=LDAP_PASSWORD
